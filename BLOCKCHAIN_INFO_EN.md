# BLOCKCHAIN INFO

This repository does not use an on-chain smart contract.

Instead, it uses `SIGNATURE.sha256` to record SHA-256 hashes for the core files in the release snapshot.

Purpose:

- verify integrity of code, docs, and scripts
- anchor the release snapshot
- support public reproducibility checks
